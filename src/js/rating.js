//in progres

// function setRating(ratingElement) {
//   const activeRatingElement = ratingElement.querySelector('.active-rating-js');
//   const valRatingElement = ratingElement.querySelector('.val-rating-js');

//   const ratingValue = parseInt(valRatingElement.innerHTML);
//   const clampedRating = Math.min(5, ratingValue);
//   const activeRatingWidth = clampedRating * 20; // 20% width ???

//   activeRatingElement.style.width = `${activeRatingWidth}%`;
// }

// function initializeRatings() {
//   const ratingElements = document.querySelectorAll('.ratings-js');
//   ratingElements.forEach(ratingElement => {
//     setRating(ratingElement);
//   });
// }

// initializeRatings();